{
  "name": "xkld-tools",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply xkld-db --local",
    "seed:admin": "tsx scripts/seed-admin.ts"
  },
  "dependencies": {
    "@hono/arktype-validator": "^2.0.1",
    "arktype": "^2.2.3",
    "hono": "^4.12.27"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.2",
    "tsx": "^4.23.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.4.0"
  }
}
